const [a, b, c, d] = [3, 5, 1, 15]

const soma = a+ b + c + d 
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // muito usado para saber se o número é par ou ímpar

console.log(soma, subtracao, multiplicacao, divisao, modulo)

// d++ operador post fix pq fica depois do operando
// ++d perador pré fix pq fica antes
// e se for a + b seria um one fix pq fica entre os dois operandos e por isso é um operador binário

